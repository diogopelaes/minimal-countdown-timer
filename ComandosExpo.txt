# Gera o .aab para google play
npx eas-cli build -p android --profile production

# Gera o apk instalável
npx eas-cli build -p android --profile preview

# ver com Expo Go
"Y" | npx expo start

# Link para instalar no celular
